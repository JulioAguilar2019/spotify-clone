import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  playList: [],
};

export const spotifySlice = createSlice({
  name: 'spotify',
  initialState: {
    initialState,
  },
  reducers: {},
});
